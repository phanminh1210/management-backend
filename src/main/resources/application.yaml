spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/group_management_system
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

server:
  port: 8111

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG


